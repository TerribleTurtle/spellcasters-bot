import { command as search } from './search';
import { command as refresh } from './refresh';
import { command as list } from './list';
import { command as compare } from './compare';
import { command as random } from './random';
import { command as about } from './about';
import { command as help } from './help';
import { command as patch } from './patch';

export const commands = {
  search,
  refresh,
  list,
  compare,
  random,
  about,
  help,
  patch,
};
